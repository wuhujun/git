关于Hbase的安装部署的博客
http://www.cnblogs.com/ggjucheng/archive/2012/05/04/2483474.html
Hbase的架构
http://www.alidata.org/archives/1509
数据模型的“”旋风之旅”，应用把数据存放在带标签的表中.
什么是列存储？列存储不同于传统的关系型数据库，其数据在表中是按行存储的，列方式所带来的重要好处之一
就是，由于查询中的选择规则是通过列来定义的，因 此整个数据库是自动索引化的。按列存储每个字段的数据聚
集存储，在查询只需要少数几个字段的时候，能大大减少读取的数据量，一个字段的数据聚集存储，那就 更容易
为这种聚集存储设计更好的压缩/解压算法。这张图讲述了传统的行存储和列存储的区别.
2.运行中的HBase 
HBase内部保留名为-ROOT-和.MEAT.的特殊目录表。他们维护着当前集群上所有区域的列表，状态和位置。
一个列名是由它的列族前缀和修饰符(qualifier)连接而成。例如列contents:html是列族 contents加冒号(:)加 
修饰符 html组成的。
Hbase应用将数据存放在带标签的表中，表由行和列组成，表格单元（cell）----由行和列的坐标交叉组成，是有版
本号，默认情况下版本号自动分配，是HBase插入单元格时的时间戳。单元格的内容是未解释的字节组成。
表中行的键也是由字节数组，所以理论上，任何东西都可以表示成二进制形式，然后转化为长整型的字符串或者直接
对数据结构进行序列化，来作为键值。表中的行根据行的键值（也就是表的主键）进行排序。排序根据字节进行。所
有的对表的访问都要通过表的主键。
     行中的列分成列簇，所有的的列簇成员都有相同的前缀，因此，像列temperature：air和temperature：dew_point
都是列簇的成员，而saation：identifier则属于station簇。列簇的前缀必须是可打印的字符组成。而修饰性的结尾字符
即列的簇修饰符可以为任意字节。
     一个表的列簇必须作为表模式定义的一部分预先给出，但是新的列簇成员可以随后按照需要加入，例如只要目标表
中已经有了列簇station，那么客户端就在更新时提供新的列station：address，并存储它的值。

